{
    "contents" : "library(data.table)\nlibrary(lattice)\n\n## Read in data\n\n## Create \"data\" directory if doesn't already exist. \nif(!file.exists(\"data\")) {\n    dir.create(\"data\")\n}\n\n## Download and unzip dataset if doesn't already exist\nif(!file.exists(\"data/activity.csv\")) {\n    temp <- tempfile()\n    fileUrl <- \"https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip\"\n    download.file(fileUrl, temp, mode=\"wb\")\n    unzip(temp, exdir=\"data\")\n}\n\n\ndata <- data.table(read.csv(\"data/activity.csv\", na.strings=\"NA\"))\n\n\n## Histogram of the total number of steps taken each day\n\n## Format date column as date\ndata$date <- as.Date(data$date, \"%Y-%m-%d\")\n\ndata_day <- data[, list(sum_steps=sum(steps)), by=date]\n\nhist(x=data_day$sum_steps, breaks=20)\n\n## Mean and median number of steps taken each day\n\n##data_day2 <- data[, list(mean=mean(steps), median=as.numeric(median(steps)), sum=sum(steps)), by=date]\n\n\nmean_steps <- mean(data_day$sum_steps, na.rm=T)\nmedian_steps <- median(data_day$sum_steps, na.rm=T)\nmean_steps\nmedian_steps\n\n## Time series plot of the average number of steps taken\n\ndata_interval <- data[, list(sum_steps=sum(steps, na.rm=T), mean_steps=mean(steps, na.rm=T)), by=interval]\n\nplot(data_interval$interval,\n     data_interval$mean_steps,\n     type=\"l\"\n)\n\n## The 5-minute interval that, on average, contains the maximum number of steps\n\nmax_interval <- data_interval[which.max(mean_steps), interval]\nmax_interval\n\n## Code to describe and show a strategy for imputing missing data\n\n# Calculate and report the total number of missing values in the dataset (i.e. the total number of rows with NAs)\n\nmissing_steps <- sum(is.na(data$steps))\n\n# Devise a strategy for filling in all of the missing values in the dataset. The strategy does not need to be sophisticated. For example, you could use the mean/median for that day, or the mean for that 5-minute interval, etc.\n\n# Create a new dataset that is equal to the original dataset but with the missing data filled in.\n\n# data_complete <-data[, `:=`( mean_steps=as.integer(round(mean(steps, na.rm=T)))), by=interval]\n\ndata_complete <- data.table(data)\ndata_complete[, `:=`( mean_steps=as.integer(round(mean(steps, na.rm=T)))), by=interval]\n\n\nfillNAs <- function(data_in){\n    data_in[is.na(steps), steps := mean_steps]\n}        \n\nfillNAs(data_complete)\n        \n# Make a histogram of the total number of steps taken each day and Calculate and report the mean and median total number of steps taken per day. Do these values differ from the estimates from the first part of the assignment? What is the impact of imputing missing data on the estimates of the total daily number of steps?\n\ndata_complete_day <- data_complete[, list(sum_steps=sum(steps)), by=date]\nhist(x=data_complete_day$sum_steps, breaks=20)\n\ncomplete_mean_steps <- mean(data_complete_day$sum_steps, na.rm=T)\ncomplete_median_steps <- median(data_complete_day$sum_steps, na.rm=T)\ncomplete_mean_steps\ncomplete_median_steps\n\n## Panel plot comparing the average number of steps taken per 5-minute interval across weekdays and weekends\n\ndata_complete$day_type <- ifelse(grepl(\"S(at|un)\", weekdays(data_complete$date, abbr=T)), \"weekend\", \"weekday\") \n\ndata_complete_weekday <- data_complete[, list(mean_steps=mean(steps)), by=list(interval, day_type)]\n#data_complete_comb <- data_complete[, list(sum_steps=sum(steps)), by=interval]\n\nxyplot(data_complete_weekday$mean_steps~data_complete_weekday$interval|data_complete_weekday$day_type, \n        main=\"Dotplot Plot by Number of Gears and Cylinders\",\n        xlab=\"Mean steps\",\n        layout=c(1,2),\n        type=\"l\"\n)\n\n\n## All of the R code needed to reproduce the results (numbers, plots, etc.) in the report\n\n\n\n\n",
    "created" : 1455464518617.000,
    "dirty" : false,
    "encoding" : "UTF-8",
    "folds" : "",
    "hash" : "1889109807",
    "id" : "E05F07DB",
    "lastKnownWriteTime" : 1454893226,
    "path" : "D:/R/Reproducible Research/Assignment1/temp.R",
    "project_path" : null,
    "properties" : {
    },
    "relative_order" : 2,
    "source_on_save" : false,
    "type" : "r_source"
}